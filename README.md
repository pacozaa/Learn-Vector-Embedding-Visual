# Visualized Vector Embeddings with Typescript

- Using `Ollama` to generate vector embedding 
- Plot it with `nodeplotlib`
- Compare it using euclidean distance.

## Usage

`npm run index "cat" "dog"`

`npm run index "cat" "cat"`
